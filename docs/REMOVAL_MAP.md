# Removal Map — scaffolding vs. product

> A companion to [DEV_JOURNEY.md](DEV_JOURNEY.md). Some files are *product* — they
> ship and must be maintained. Others were only ever **scaffolding**: reasonable
> *while* you were building something, dead weight once it's built. This file
> separates the two so the repo stays as light as the product actually is.

## The principle

Ask of every file: **does the product need this, or did the *act of building it*
need this?**

- **Product** — the library (`pump/`), the workbench (`pumpflow/`), the spec
  (`UI_SPEC.md`), the decisions (`docs/adr/`, `docs/sprints/`, `docs/product/`),
  the canonical reference notebook, and a *small* set of bundled examples. Keep.
- **Scaffolding** — study material you vendored to learn from, smoke notebooks
  you ran once to check a setup, build artifacts, generated outputs, and
  divergent scratch copies. Remove once their job is done.

Nothing below has been deleted — this is the map. Act on a row when you're ready,
ideally folding it into the sprint it belongs to.

## Tier 1 — Safe to remove now (regenerable artifacts)

Already git-ignored; they're just clutter on disk. Each regenerates from one
command, so deleting loses nothing.

| Path | Size | Regenerated by |
|---|---|---|
| `dist/` | 56 K | `python -m build` |
| `centrifugal_pump.egg-info/` | 28 K | `pip install -e .` |
| `.pytest_cache/` | 32 K | `pytest` |
| `docs/_build/` | 7.4 M | `sphinx-build -b html docs docs/_build/html` |

```bash
rm -rf dist centrifugal_pump.egg-info .pytest_cache docs/_build
```

> Note: `docs/_build/` still holds a **stale** copy of `mathx.py` with the old
> `water_density_kgm3` (now replaced by `pump.Water`). Another reason to clear it.

## Tier 2 — Remove: vendored dev scaffolding (no longer referenced)

| Path | Size | Why it can go |
|---|---|---|
| `sample_repo/orange3` | **120 M** | Orange3 source, vendored while studying the node-canvas pattern. `pumpflow` was built **natively on PySide6 with no Orange3 runtime** (CLAUDE.md). **Zero references** anywhere in source, docs, or config. It's tracked as an embedded git repo (gitlink). |

This is **94% of your reclaimable weight.** It's a learning reference, not product.

```bash
git rm --cached sample_repo/orange3      # untrack the gitlink
rm -rf sample_repo
echo "sample_repo/" >> .gitignore        # belt-and-suspenders
```

## Tier 3 — Remove: divergent duplicate

| Path | Why it can go |
|---|---|
| `use_cases/utilities_test.ipynb` | A tracked **second copy** of the canonical `tests/utilities_test.ipynb`, and it has **diverged** (the files differ). The sprint workflow names `tests/utilities_test.ipynb` as the single demo-source ([sprints/README.md](sprints/README.md)). Two diverging copies of the source-of-truth notebook is a trap. |

```bash
git rm use_cases/utilities_test.ipynb
```

## Tier 4 — Decide: dev-time smoke scaffolding

| Path | Status | Recommendation |
|---|---|---|
| `tests/00_pip_test.ipynb` | untracked | A "did the install work?" notebook. Useful during setup, but `tests/test_pump_smoke.py` now covers import/smoke as a real assertion. **Drop it** (or keep unstaged, never commit). You already deleted its predecessor `tests/pip_test.ipynb`. |

## Tier 5 — Don't commit: generated report outputs

`examples/` mixes **bundled sample inputs** (keep) with **generated `.docx`
outputs** (don't track). 8 `.docx` are on disk; 3 are tracked. The untracked
`*_12-06-2026.docx` pair are outputs of running a notebook — regenerable, not
source.

| What | Action |
|---|---|
| Untracked generated `.docx` (e.g. `Report_B-4300.22101AB_12-06-2026.docx`) | Leave untracked; add a pattern so they're never committed. |
| The 3 tracked `.docx` | Decide: keep **one** as a canonical "what the report looks like" sample, or move all under `examples/reports/` and git-ignore that folder. |

```bash
# suggested: keep example reports out of version control
echo "examples/*.docx" >> .gitignore
```

## Tier 6 — Already on the roadmap (don't do ad-hoc)

| Path | Why it stays *for now* |
|---|---|
| `Point` class — `pump/point.py:240` | Genuinely dead (broken `outlet_pressure`, no external users; CLAUDE.md says "treat as dead code"). But its removal **already has a home**: [ADR 0005 — consolidate the Point hierarchy](adr/0005-consolidate-point-hierarchy.md), scheduled as **S9** in [DEV_JOURNEY.md](DEV_JOURNEY.md#the-next-sprints-sequenced-for-you). Remove it *there*, with the tests, not as a one-off. |

## Lower-confidence candidate (verify before acting)

| Path | Note |
|---|---|
| `examples/exploratory_performance.ipynb` | Only referenced from `CHANGELOG.md`; the in-app **Performance Explorer** (recent commits) appears to supersede its purpose. Confirm it's not a teaching example you still want before removing. |

## Bottom line

Tiers 1–3 are clean wins reclaiming **~127 MB** (almost all from `sample_repo`)
with zero product risk — do them in one tidy "repo hygiene" commit. Tiers 4–5 are
quick judgment calls. Tier 6 is already sequenced — leave it to its sprint.
