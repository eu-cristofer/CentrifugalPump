# Sprint S0 — Product context & sprint scaffold (8 h)

- **Goal:** Record the corrected audience + use-case registry and stand up the
  sprint system, so all later work has a single source of truth for *who* and *why*.
- **Persona / UC:** all personas (foundational) · no single UC.
- **Branch / commit:** `orange` — one commit. **No production code.**

## Tasks (≈ 8 h)

- [x] Write [`docs/product/audience.md`](../product/audience.md) — personas re-anchored
  to files that exist in this repo — ~2h
- [x] Write [`docs/product/use-cases.md`](../product/use-cases.md) — UC registry with
  status against the current library + MVP/v1.1/out-of-scope priorities — ~2.5h
- [x] Write [`docs/sprints/README.md`](README.md) — sprint index + reusable template — ~1.5h
- [x] Author the six sprint stubs S1–S6 from the template — ~2h

## Files touched

- Create: `docs/product/audience.md`, `docs/product/use-cases.md`,
  `docs/sprints/README.md`, `docs/sprints/sprint-0{0..6}-*.md`.

## Acceptance criteria

- Audience + UC registry committed and internally consistent.
- Every "PumpLabGUI / screen-*.jsx" reference is mapped onto `pumpflow/`, and the
  repo-vs-spec discrepancy is recorded explicitly.
- All seven sprint files exist; each has a checklist whose estimates sum to ~8 h.

## Definition of Done

- [x] Product docs + sprint scaffold written and committed.
